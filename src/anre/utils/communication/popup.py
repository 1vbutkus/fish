def popupMsg(text: str, title: str | None = 'Message'):
    # FIXME TODO popupas pasirodogali uzlausineti pythona. Tai reikiarasti solutiona, kuris uztikrintai nelaustu

    print(
        "==================================================================================================================================="
    )
    print(
        "==================================================================================================================================="
    )
    print(title)
    print(text)
    print(
        "==================================================================================================================================="
    )
    print(
        "==================================================================================================================================="
    )

    # fig, ax = plt.subplots()
    # ax.set(frame_on=False)
    # ax.axis('off')
    # ax.text(0.5, 0.5, text, horizontalalignment='center', verticalalignment='center', color='red', fontsize=20, fontweight=6)
    #
    # if hasattr(fig.canvas.manager, "window"):
    #     fig.canvas.manager.window.setGeometry(1252, 230, 2170, 1441)
    #     if title is not None:
    #         fig.canvas.manager.window.setWindowTitle(title)
    # else:
    #     msg = f'Popup message: {text}'
    #     print(msg)
    #     logging.warning(msg)
